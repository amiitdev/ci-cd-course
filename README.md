# CI/CD Course

Learning GitHub CI/CD from zero.

- **ES6 modules** (`import` / `export`) — requires Node.js ≥ 16, no build step.
- Tests use Node's built-in **`node:test`** — zero dependencies.

## Run

```bash
node index.js        # start the app
npm test             # run the test suite
```